import React from 'react';
import './VideoDetails.css';

const VideoDetails = () => {
    return (
        <main className="video-details">
            <h2>React Basics</h2>
            <p>This video covers the basics of React, including components, props, and state.</p>
        </main>
    );
};

export default VideoDetails;
